import { Text, View } from 'react-native';

export const EditScreenInfo = ({ path }: { path: string }) => {
  return (
    <View>
      <Text className="text-2xl text-green-500">Hi</Text>
    </View>
  );
};
